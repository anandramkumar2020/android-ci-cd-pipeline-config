pipeline {
    agent any

    stages {
        stage('App-Build') {
            steps {
                echo 'Build Application'
            }
        }
        stage('App-Test') {
            steps {
                echo 'Test Application'
            }
        }
        stage('App-Deploy') {
            steps {
                echo 'Deploy Application'
            }
        }
    }
}
